import { sharedSubcontractGroupUiSubcontract } from './shared-subcontract-group-ui-subcontract';

describe('sharedSubcontractGroupUiSubcontract', () => {
  it('should work', () => {
    expect(sharedSubcontractGroupUiSubcontract()).toEqual(
      'shared-subcontract-group-ui-subcontract'
    );
  });
});
